@model IEnumerable<Capstone.Models.TripItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Zoo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Trip)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Zoo.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Trip.Name)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TripItemId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TripItemId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TripItemId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
